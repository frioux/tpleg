{{define "begin"}} this is the default begin {{order}} {{end}}
{{define "end"}} this is the default end {{order}} {{end}}
{{define "inner"}} this is the default inner {{order}} {{end}}

BEGIN: {{template "begin"}}

INNER: {{template "inner" }}

END: {{template "end"}}
